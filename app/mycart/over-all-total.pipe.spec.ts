import { OverAllTotalPipe } from './over-all-total.pipe';

describe('OverAllTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new OverAllTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
